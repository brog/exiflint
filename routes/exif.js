exports.exif = function(req,res){
   /*
    try {
        new ExifImage({ image : 'images/4765114690_d5e47ddbf8_o.jpg' }, function (error, image) {
            if (error){
                console.log('Error 1: '+error.message);
            }else{
                res.render('exif', {title : 'Exif Lint', lint : image});
                //res.json(200, image);
                //res.send( 'image 3\n' + JSON.stringify(image, null, 4) );
                console.log(image); // Do something with your data!
            }
        });
    } catch (error) {
        console.log('Error 2: ' + error);
    }
*/

};